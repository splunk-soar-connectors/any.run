* Added an `http_proxy` asset configuration parameter to route ANY.RUN API requests through an HTTP proxy
* Bumped the minimum supported SOAR platform version to 7.0.0 and the required Python version to 3.13
* Updated to anyrun-sdk version 1.14.18, requests 2.34.2, and splunk-soar-sdk 4.2.1
* Fixed `search_analysis_history` searching the team's analysis history instead of the user's own
* Fixed `get_reputation` and `get_intelligence` ignoring the `lookup_depth` parameter when building the ANY.RUN lookup URL
* `get_report` now defaults to the `json` report format instead of `summary`
* Removed the unsupported `obj_ext_extension` and `obj_ext_browser` parameters from the Android URL detonation action
* New analyses are no longer tagged with `splunk-soar-analysis` by default